export class Author {
  id: number;
  firstName: string;
  surName: string;

  constructor(id: number, firstName: string, surName: string) {
    this.id = id;
    this.firstName = firstName;
    this.surName = surName;
  }
}
